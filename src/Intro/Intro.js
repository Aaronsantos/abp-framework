import React, { Component } from 'react';

export default ({ step }) => {
    return (

        <main className="etapa ">
            <h2 className="title" >{step.nome}</h2>
            <hr />
            <div className="etapa-container">
                <section className="card desc"> olá </section>
               
            </div>
        </main>
    );

}